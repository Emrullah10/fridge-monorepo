---
// fridge-mobil/lib/core/widgets/app_bottom_nav.dart deseni: 5 sekme, sadece
// "Dolabım" gerçek, diğerleri pasif ("yakında" hissi). İkonlar gerçek
// uygulamadaki Icons.*_rounded adlarıyla birebir (bkz. cerebrum.md).
import Icon from '@components/site/Icon.astro';

/**
 * @typedef {Object} Props
 * @property {'areas'|'shopping'|'recipes'|'insights'|'settings'} [active]
 */
const { active = 'areas' } = Astro.props;

const tabs = [
  { key: 'areas', icon: 'grid_view', label: 'Alanlarım' },
  { key: 'shopping', icon: 'shopping_cart', label: 'Alışveriş' },
  { key: 'recipes', icon: 'menu_book', label: 'Tarifler' },
  { key: 'insights', icon: 'savings', label: 'Para' },
  { key: 'settings', icon: 'settings', label: 'Ayarlar' },
];
---
<nav class="mock-bottomnav">
  {tabs.map((tab) => (
    <div class:list={['mock-bottomnav__tab', { 'is-active': tab.key === active }]}>
      <Icon name={tab.icon} size={20} />
      <span class="mock-bottomnav__label">{tab.label}</span>
    </div>
  ))}
</nav>

<style>
  .mock-bottomnav {
    display: flex;
    justify-content: space-around;
    align-items: center;
    padding: var(--space-sm) var(--space-xs);
    background: var(--surface);
    border-top: 1px solid var(--outline-variant);
    position: sticky;
    bottom: 0;
  }
  .mock-bottomnav__tab {
    /* Pasif sekmeler token rengiyle (opacity ile soldurulmadan) ayrışır —
       10px etiket metninde opacity dimming AA kontrastını düşürüyordu
       (bkz. cerebrum.md Do-Not-Repeat). */
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2px;
    color: var(--on-surface-variant);
  }
  .mock-bottomnav__tab.is-active {
    color: var(--primary);
  }
  .mock-bottomnav__label {
    font-size: 10px;
    font-weight: var(--fw-semibold);
  }
</style>
